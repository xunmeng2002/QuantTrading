# 项目进度跟踪

> 按 Harness 规则 8 维护：每次会话结束更新。新会话开始先读本文件。

## 项目定位

CTP 期货量化交易系统（C++20），当前处于**前期整理阶段**：行情服务 **MdOffer**、回测引擎 **BackTest**（C++/Python 双端策略）、模拟盘 **SimExchange** 均已落地并经单测/冒烟验证；实盘交易链路（TraderApiMiddle）已封装但未接入应用。

## 🗂 归档索引

> `PROGRESS.md` 只保留活内容：🔄 进行中、❓ 未决、最近 3 批 ✅。已关闭与已了结的条目**原文**在 [`PROGRESS-archive.md`](PROGRESS-archive.md)（2026-09-13 拆分，不删不改）。

- 检索：`grep "关键词" PROGRESS-archive.md`（例：`grep "TradeSessions" PROGRESS-archive.md`）。
- 条目 ID：`D.*` = 原 ✅ 已完成（`D.00`–`D.49` 已归档）；`Q.*` = 原 ❓ 待讨论；`R.*` = 原 🔄 进行中。ID 取自 2026-09-13 拆分时的文档顺序，此后新归档条目按 `D.49`、`D.50`… 递增。
- **批次号 → 归档 ID 对照**（主文件正文里出现"第 N 批"时按此换算）：第二批 `D.32`(2026-09-10) / `D.35`(2026-09-12)、第三批 `D.36`、第四批 `D.37`、第五批 `D.38`、第六批 `D.40`、第七批 `D.41`、第八批 `D.42`、第九批 `D.43`、第十批 `D.45`、第十一批 `D.46`、第十二批 `D.47`、第十三批 `D.48`(2026-09-17)、第十四批 `D.49`(2026-09-17)。
- 必须查归档的时机：引用 2026-09-12 及更早的结论时；复审某个已关闭事项时；主文件某条写明"见归档 `Q.xx` / `R.xx`"时。
- 滚动规矩：已完成区超过 5 批时，把最旧的整条**原样**移入归档（主文件目标 ≤ 50 KB）。

## ✅ 已完成

> 更早的 50 条（`D.00`–`D.49`，2026-08-08 ~ 2026-09-17）已归档，见 [`PROGRESS-archive.md`](PROGRESS-archive.md)。已滚动七次：第九批（`D.43`）、分层归档条目（`D.44`）、第十批（`D.45`）、第十一批（`D.46`）、第十二批（`D.47`）（以上 2026-09-17 搬入）、第十三批（`D.48`，2026-09-18 搬入）、第十四批（`D.49`，2026-09-18 搬入）。

- **2026-09-18（第十五批）回测运行契约 + 手续费全链路分列 + 基本数据 InitDb（A3 / A4 / A5 一次性落地）**：
  - **背景与授权**：平台化的三个缺口——① **拿不到结果**（`test/TestBackTest/Main.cpp:31` 无条件 `return 0`，且 `api->Init()` 的返回值在 `:24` 被丢弃，"初始化失败"被伪装成"成功"）；② **费用恒 0**（`Trade.Commission` 唯一写入点是 `OrderMatch.cpp:141` 的硬编码 `= 0`，而 `Settlement.cpp:127` 的权益公式本身是对的，只因上游恒 0 而永不起作用）；③ **无基本数据**（`Product` 空表 → `VolumeMultiple` 兜底 1，第十四批已实测）。用户 2026-09-17 授权三块一起做（"A3、A4、A5 都开始做吧"，并指示**实现语言优先 Python**——宿主与种子脚本走 Python，引擎侧仍是 C++）。前置的仓外字典与两张表定义见归档 `D.49`。
  - **A4 计费（全链路分列；用户拍板"一并补印花税/过户费"，闭环见归档 `Q.20`）**：
    - 新增 `src/Settlement/CommissionCalculator.{h,cpp}`：按 `(手续费组, 交易所, 合约, 方向)` 取费率行，`佣金 = TradeAmount × 金额侧费率 + Volume × 手数侧费率`（**两项相加**，两条腿各一列），再按 `MinCommission`/`MaxCommission` **按每笔成交**封底封顶且**只封佣金**（`<= 0` 视为不限）；印花税/过户费各行其列、按法定费率实收。费率行缺失或表未注册时按 0 计费、**不阻断成交、不抛异常**，按 `(组|交易所|合约|方向)` 去重告警并计数：`GetMissingRateCount`（按笔）、`GetZeroRateKeyCount`（按键）、`GetMissingRateKeys`（上限 200 条，供 `result.json` 抄缺口）。**已知后果**：N 笔部分成交最多产生 `N × MinCommission`（整单封顶要回溯改写已 `Insert`/已回报的 `Trade`，属"成交回报不可变"的例外，留作后续）。
    - **四张表各追加 `StampTax`/`TransferFee` 两列**（`Trade`/`PositionDetail`/`Position`/`Capital`，一律追加在 items 末尾——`DbAdapters` 是 `SELECT *` + 列下标绑定，插中间会让旧库静默错位）。`Commission` 从此**忠实只装佣金**；`Settlement::CalcCapital` 的 `Balance` 改为减三项，`Available` 公式**未改**（本批不加 `FrozenStampTax`/`FrozenTransferFee`——`FrozenCommission` 今天全链路只有置零、无写入，给没人冻结的费用加冻结列属投机）。
    - **三条写入路径**：开仓腿 `OrderUtility.cpp:185/:206` 各补抄两列（自动跟随）；**平仓腿是本批唯一的真缺口**——`PositionMaintenance.cpp:62-102` 一个 `Commission` 都不写，而 `Settlement.cpp:73` 会把 `position->Commission` 归零重建、`:87` 只认明细汇总，故平仓手续费至今**收不到钱**。修法：循环内按成交量分摊三项，**末笔（`currVolume == remainVolume` 的整数比较）直接取余额而非按比例算**，保证 `sum(各明细分摊) ≡ trade` 的三项在 IEEE double 下**逐位闭合**（已用 Python 对 1/3 与 1/5 两种分摊逐项验证：三项各自之和 `== trade` 严格成立）；并**追加**"持仓不足、余额无承载体时落回最后一条已配对明细"的兜底——属严格超集，不改变任何已批准路径的行为（计划原文称"唯一合法丢失路径是一笔明细都没配对"，实际持仓部分不足时余额同样无承载体，这是对计划盲点的修补而非改道）。结算三级汇总（`Settlement.cpp` 四处置零/累加）与 `SettlementRoll.cpp`、`SimExchangeInit/Init.cpp` 的置零同步补齐。
    - **注入点**：`SimExchange::OnTrade` 第一行（回测与实盘各一行，同一实现两处调用）。**必须早于 `SendRtnTrade`**（`TradeField` 带 `Commission`，晚了策略的 `on_trade` 看到的就是 0）且早于 `positionMaintenance_->UpdateOnTrade`（开仓腿靠 `CreatePositionDetail` 抄、平仓腿靠上面的分摊）。**实盘清零**：`SimExchangeTableNames.xml` 刻意不改 → 实盘 `mdb->BaseCommission == nullptr` → 空守卫直接返回、三列保持 0，**实盘行为逐字节不变**（代价是实盘侧不计费，见 ❓）。
    - `CommissionRate`（公开 C ABI `CommissionRateField` 的 8 个槽位）按用户裁定**就地换成 8 个新名**（甲），结构体布局与 `sizeof` 不变；`test/ApiMiddles/TraderApiMiddle/TraderSpiMiddle.cpp:231-232` 的逐字段日志照换。**遗留**：字段 id 序列随之由 `0x5006`–`0x500D` 前移为 `0x5002`–`0x5009`（8 个旧条目被就地替换、`0x500A`–`0x500D` 被删，逐槽位保值在数学上已不可能）；本仓两端同批重生成、自洽，**仓外若有老对端进程会按 id 解错槽位含义**；本批不做协议版本号。
  - **A3 结果契约**：新增 `src/BackTest/RunResult.h`（header-only，PascalCase 键名与成员一一对应，`SchemaVersion = 1`，含 `ToJsonValue`/`WriteRunResultFile`/`ExitCodeFromRunResultFile`）。引擎在 `OnMdEnd` 的 **`mdb_->Dump()` 之后、`SendRtnMarketDataEnd()` 之前**写到 **`<CWD>/result.json`**（固定名——宿主拿不到只在引擎配置里的 RunId，而"一 job 一 cwd"下固定名无歧义，产物身份由文件内的 `DumpPath`/`DbPath`/`RunId` 反向给出）；**行数一律取自内存表**（`PrimaryKey->SelectAll()` + `std::distance`），**绝不查输出库**（`dbWriter_` 是异步的，收尾时并不完整）。
    - **退出码**：`0` 成功 / `1` 宿主启动失败 / `2` 结果文件缺失或不可解析 / `3` 引擎报告失败。三个宿主（`TestBackTest`、`TestStrategyGrid`、`grid_strategy.py`）**启动策略前先删旧 `result.json`**，让"文件不存在 ⇒ 本轮没走完收尾"成为硬保证（调度器复用工作目录不会读到上一轮残留）。**已知歧义**：MSVC 下 `abort()`/未捕获异常同样返回 3，与"引擎报告失败"同码，消歧只能靠文件是否存在。
    - `SetError` **幂等（首个置位者胜）**——`OnMdEnd` 里还有派生判据，不幂等会被后面的判据覆盖成因。只置两处（bar 周期无法聚合、无任何有效订阅），另加一条**派生判据**兜住"订阅成功但 parquet 零行"（该情形与正常跑完在日志上完全不可区分）。部分降级（部分订阅被丢、部分合约查不到、tick 早于当前交易日、费率缺失）**一律不置错误**——置了会把合法场景误判为失败。
    - **顺带修掉 `TestBackTest/Main.cpp` 的真 bug**：`api->Init()` 失败时引擎线程从未启动，原代码仍对未启动的 `AsyncDbWriter`/`ThreadBase` 调 `Join()`，最后无条件 `return 0`——"初始化失败"被伪装成"成功"。加返回值检查后失败立即返回 1。
    - **本批不给 `ThrowIfQueryFailed` 加 try/catch**：`MdReader.cpp` 的 `std::logic_error` 在引擎线程内抛、全仓无捕获点 → `std::terminate`；捕获后调 `OnMdEnd()` 等于在**半装载状态**上跑 `Settlement()`/`Dump()`，反而更像"失败伪装成成功"。可识别信号是"非零退出码 + 无 `result.json`"。
  - **A5 基本数据（走甲：只灌 `Product` + `CommissionGroup` + `BaseCommission`）**：新增手写 `src/BackTest/BackTestInitDbTableList.h`（`BackTestTableList.h` 是 pump 产物会被覆盖，故另开手写文件）。**不传完整 `BackTestTableList`**——全量会把 `Account`/`Capital` 灌进来，与 `HandleRegisterAccount` 的按需自建**正面冲突**（`Account` 行被抢注后走 else 分支不再建 `Capital`，而灌入的 `Capital` 行交易日是种子里 的历史值、`SettlementAccount` 按当日索引查不到 → 种子资金被静默忽略，比不灌更糟）；也不含 `Instrument`（会被行情侧整行 `Update` 覆盖）。**这正是 commit `7903c90` 那套被拆除的根源。**
    - `LoadBasicDataFromInitDb` 在构造期一次做完"`std::filesystem::exists` 检查 → `CreateDataDb` → `Connect` → `LoadTables` → `DisConnect` → `delete`"，**不留成员**（`7903c90` 那份把 `m_InitDB` 存成成员却全程没有一处 `delete`，是确定性的资源泄漏）；**不能靠 `Connect` 失败判断缺库**（SQLite 会静默建空库）。三级降级都不让回测失败：缺库/空路径 → Warning + `BasicDataLoaded=false`；连不上 → Error + false；连上没有那两张表 → 0 行，等价于"费率行缺失"，由 `MissingRateCount` 在 `result.json` 里现形。
    - 配置新增三项（`Model/Configs/BackTest.xml` + `Configs/BackTest.json`）：`DbInitHost`、`InitialCapital`、`CommissionGroupId`。**`CommissionGroupId` 是费率组真正可切换的唯一途径**——此前 `HandleRegisterAccount` 硬编码 `1`，整套组号机制存在却永远无法变化，"只换参数"就落不了地。`InitialCapital` 落到 `Capital` 行的 `PreBalance`/`Balance`/`Available`/`Deposit`（该行原先 `memset` 后全 0）。
    - `VolumeMultiple` 兜底**不改成失败**（股票口径下 1 本来就是对的，改成失败会让每次股票回测都炸），改为在外层按 `(交易所, 品种)` 分组告警一次 + `++volumeMultipleFallbackProductCount_` 进 `result.json`——这才是"不修则 InitDb 上线后仍静默退化成 1"的堵口。
  - **新增仓根脚本 `makeseeddb.py`**（只用 stdlib `sqlite3`，**不新增第三方依赖**）：写出 `bin/<CONFIG>/BackTestInit.db`（`/bin` 已被 `.gitignore` 覆盖，二进制产物不进版本库）。内置 A 股最小种子：`CommissionGroup(1, "Default")`；`Product` 为 `(SSE, 600)` / `(SZSE, 000)` 且 **`ProductClass = Stock(6)`**（写成 `Future` 会让 `InitMainInstrument` 凭空造出 `.Hot/.Second/.Third` 一批主力合约），`VolumeMultiple = 1`、`SessionName` 留空（引擎不消费该字段；副作用是 `t_Instrument.csv` 的 `SessionName` 从兜底值 `FD0900` 变为空，属良性变化）；`BaseCommission` 三只标的 × 买卖两行，**卖出单边印花税写在 Sell 行的平仓侧**（买卖差异由数据表达，不写进代码）。可选第二式从 CSV 目录读行——**表头即列序，与预期列序不符即拒绝**，防的正是"列序错静默错位"。
    - **已生成并核对**（脚本实跑）：`BaseCommission` 14 列、末 4 列是印花税/过户费 4 列、**第 4 列是 `Direction`**，与 `src/Mdb/MdbStructs.cpp` 的字段序逐列一致；`PRAGMA table_info` 三表列名全部对齐。CSV 第二式做了往返验证（导出再导入，行数一致）与表头不符的拒绝验证（退出码 1）。
  - **新增 `docs/backtest-run-contract.md`**：落定工作目录假设（**引擎全部输入与产物路径都相对 CWD**——这是"每 job 独立工作目录"隔离的**全部依据**，谁把任一路径写成绝对路径隔离就**静默失效**，两个并发 job 写同一个库且不报错）、`result.json` 全键表、`ErrorId` 取值（只有 `0x100F` 判失败）、`MissingRateKeys` 键格式、退出码表 + 陈旧结果防护 + 同码歧义、种子库生成与"首轮发现式播种"闭环、费用口径。
  - **A4.5 单测**：新增 `test/UnitTests/CommissionTestHelpers.h`（计费专用最小 `TableList` + `InsertCommissionAccount`/`InsertBaseCommission`/`MakeCommissionTrade`/`CollectCommissionPositionDetails` 夹具——**不能复用 `SimExchangeTableList`**：实盘清单刻意不含费率表，复用会让 `mdb->BaseCommission` 恒 `nullptr`，计费一步都跑不进去）+ `CommissionTests.cpp`（`TEST_SUITE("Commission")` **10 个用例**：金额费率 2.5、两列相加而非相乘、按笔封底、负 `Max` 不封顶、封底只封佣金、三条明细分摊逐位闭合、持仓不足余额落末条、缺失费率计数与去重键 `"1|SSE|600000|0"`、买卖方向参与取行、**回测表清单已注册费率两表**）。最后一条是"改了 `BackTestTableNames.xml` 却忘了重跑 `pumpall.py`"的**唯一自动防线**，为此给 `UnitTests` 加了 `src/BackTest` include 目录（`add_shared_module` 的 `src_dir` 只是 PRIVATE）。
  - **测试 I/O 示例（§7）**：单测用例 1 的输入输出——
    ```text
    输入：Buy+Open，Price 10.0 × Volume 1000 × VolumeMultiple 1 → TradeAmount 10000
          BaseCommission(组 1, SSE, 600000, Buy).OpenByMoney = 0.00025，其余列 0
    输出：trade->Commission == 2.5；trade->StampTax == 0；trade->TransferFee == 0
    ```
    端到端验收（需用户在 VS 构建后执行）：`cd bin/Release && python grid_strategy.py ; echo $?`（**不是** `../test/...`：CWD 必须是 `bin/Release` 供引擎读 `BackTest.json`，而 `../` 从那里只上溯到 `bin/`、没有 `test/`；脚本由 CMake 拷进 `bin/Release`，就地跑即可） —— 期望退出码 `0`、产物目录新增 `t_BaseCommission.csv`(6 行)/`t_CommissionGroup.csv`(1 行)、`t_Trade.csv` 的 `Commission` 列被封底到 5.0（种子 `MinCommission = 5` 且每笔成交 1 手），且 `t_Capital.csv` 末行满足 `Balance == PreBalance + 盈亏 − Commission − StampTax − TransferFee`。
  - **风险（§7）**：无多线程/锁/内存管理改动。四条契约级变化——① `Trade`/`Capital` 的 `Commission` **语义变更**（从"唯一费用"变为"只装佣金"），本批产物与既有产物**不可直接比较**（刻意如此，为的是不让名字撒谎）；② 计费进入 `OnTrade` 首行与结算汇总路径，属撮合→结算的**性能敏感路径**（每笔成交多一次主键查表 + 三次乘加，规模可忽略）；③ 缺种子/缺费率行**不再有任何响亮失败**，全靠 `result.json` 的计数与 `MissingRateKeys` 现形——**这是本批最需要盯的静默面**；④ A5 会让股票回测的 `ProductClass` 从兜底的 `Future(0)` 变为真值 `Stock(6)`，`PositionMaintenance` 的 `CashIn`/`CashOut` 分支**首次生效**（`Balance` 公式不含这两项，故权益数值不受影响，只影响那几个 CSV 列从 0 变成真值——算修正而非回归）。
  - **注释披露（§4）**：本批新增约 12 处注释，均为"命名无法表达的外部前提或历史成因"，逐条披露：`CommissionCalculator.cpp` 4 处（Min/Max `<=0` 视为不限且只封佣金、实盘未注册费率表故空守卫、方向不参与取列而由数据表达、两列相加非相乘）、`PositionMaintenance.cpp` 3 处（平仓腿才落费用、末笔取余额保证逐位闭合、持仓不足时余额落末条并指出该情形已有告警）、`SimExchange.cpp` 3 处（InitDb 清单为何不含 Account/Capital、费率表缺失不判失败、派生判据兜零行情）、`BackTestInitDbTableList.h` 1 处（手写非生成 + 清单取舍理由，含 `Instrument` 会被整行覆盖）、`makeseeddb.py`/`docs` 若干（列序即字段序、`ProductClass` 必须为 Stock）。**如需删除或改写请指出。**
  - **未验证**：**AI 未编译、未运行任何 C++ 代码**（按约定，构建在 VS 侧）；单测 10 个用例是**静态编写、未编译未跑**，其中"三条明细分摊逐位闭合"的浮点结论已用 Python 同构验证（1/3 与 1/5 两种分摊的三项之和严格 `== trade`），但 C++ 侧的执行顺序/优化差异未经验证。`makeseeddb.py` **已实跑**（种子库已生成并核对，CSV 往返与拒绝路径已验），产物在 `bin/Release/BackTestInit.db`。**2026-09-18 用户侧已验**：编译通过，C++ 宿主与 Python 宿主均端到端跑通、`result.json` 正常写出（复核经过见 ✅ 第十六批）。
  - **提交状态**：**已提交** `4fab69c`（2026-09-18，与本批并含第十四批的 18 个文件；共 71 个文件、+3165/−338）。`Model/TableNames/SimExchangeTableNames.xml` 刻意未改。**AI 未推送**（按约定）。

- **2026-09-18（第十六批）result.json 口径修正：三项费用改整轮累计 + 新增 MarketDataType（`SchemaVersion` 1 → 2）**：
  - **背景**：用户编译并端到端跑通后告知"能正常执行"，AI 复核产物时发现引擎回报的费用口径不对——**不是引擎跑错，是报出来的口径错了**。
  - **缺陷（已修）**：`Commission`/`StampTax`/`TransferFee` 取的是 **`LastTradingDay` 当日**的资金行，而 `Capital` 是**逐日流量表**——`Settlement::SettlementAccount` 每天先把三列清零、再只汇总当日（`Settlement.cpp:35-37` 与 `:50-52`；其下的 `Position`（`:77-79`/`:93-95`）与 `PositionDetail`（`:85`，按 `TradeMatchIndex->EqualRange(tradingDay, …)` 当日过滤）同理）。**实证**：`Dump/20260918_194031_140` 那一轮 84 笔成交、实收 **Commission = 420.000000** 元（`t_Trade.csv` / `t_PositionDetail.csv` / `t_Capital.csv` 三种全部行求和一致），而末行（20241231，当日无平仓）三列**全 0**——旧口径会把整轮费用报成 0。**修法**：新增 `SimExchange::AccumulateAccountFee()`（按账户把全部 `Capital` 行相加，复用 `Settlement::TradeFee` 与 `AddTradeFee`，不另造结构），`BuildRunResult` 据此填 **`TotalCommission`/`TotalStampTax`/`TotalTransferFee`**（改名以标明作用域）；`Balance`/`Available` 仍是期末状态值，**不改名**（与 CTP 字段名保持镜像），只在契约文档补口径说明。
  - **语义更正（另一处误导）**：新增 `MarketDataType`（`Bar`/`Tick`），因为两个行情计数**互斥非零**——`SimExchange.cpp:129` 按 `MatchMode` 只消费一种行情，Bar 模式根本不写 tick 表，故 `DepthMarketDataCount` **恒为 0**（本轮两轮产物的 `t_DepthMarketData.csv` 都只有表头）；且 tick 表按 `(交易日, 交易所, 合约)` 主键就地更新（`:497-505`），它是**每日每合约一条快照**，不是 tick 条数。契约 §2 原描述「tick 行情表行数」会让人读成本轮没有 tick 数据。
  - **文档**：`docs/backtest-run-contract.md` §2 键表同步（含三条注：口径=状态值 vs 整轮累计、两个计数互斥非零、零成交时费率缺口三项是真空的 0 而非"费率齐全"）。
  - **测试输入输出示例**：`Dump/20260918_194031_140`（84 笔成交）——修复后 `result.json` 的 `TotalCommission` 应为 `420.0`、`TotalStampTax` 应为 `4.297395`、`TotalTransferFee` 应为 `1.341961`（`Capital` 全部行求和口径）；同一轮在旧口径下三列均为 `0.0`。零成交轮（`TradeCount: 0`）三列应仍为 0。
  - **补记（同日）："result 还是不对"的真因不在引擎，在 Python 策略缺 `on_bar`**。用户 2026-09-18 晚重跑数轮后报"result 数据还是不对"，复核产物定位到：**同一份配置、同一份数据，C++ 宿主 `TestStrategyGrid` 跑出 654 委托 / 84 成交，Python 宿主 `grid_strategy.py` 每次都 0 委托 0 成交**（`Dump/20260918_194031_140` C++=84；`:194058_440`、`:194155_711`、`:180912_511` Python=0；日志侧 `TestStrategyGrid.*` 931.6K 对 `grid_strategy.*` 20.6K，两条线完全稳定可复现）。**根因**：`Configs/BackTest.json` 是 `MatchMode: Bar`，引擎在 Bar 模式**从不回调 `OnTick`**（`GridStrategy.cpp:46` 的注释已写明"Bar 回放模式无 tick 回调"），而 C++ 策略**同时实现了 `OnTick` 与 `OnBar` 两个锚定入口**（`:29`/`:47`，先到先锚），Python 策略 `grid_strategy.py` 却**只定义了 `on_tick`**——于是它一次都没锚定（日志 `Anchor price` 出现 0 次）、没挂任何梯子。**引擎报的 0 是忠实的**，费率三项为 0 也是对的（零成交时确无费用）；第十六批的费用修正**本身已验证正确**：C++ 那一轮的 `Capital` 全行求和恰为 `420.0 / 4.297395 / 1.341961`，与修法预期逐位吻合，而末行三列全 0 正是旧口径会报 0 的原因。**修法**：给 `grid_strategy.py` 补 `on_bar`，与 C++ `OnBar` 一一对应（`awaiting_anchor` 守卫 → `bar_data.close <= 0` 返回 → 消费锚定标志 → `place_ladder`）。**附带发现（未改）**：`QuantTradingBindings.cpp:143` 的 `DispatchHookWithGil` 在 Python 类未覆写某钩子时**静默走基类空实现、不告警**（`py::get_overload` 返回空即 `fallback()`），这正是本例能潜伏这么久的原因；建议按钩子名**只告警一次**（Bar 模式下次序回调 2928 根，逐次告警会刷屏），但属共享绑定层行为变更，**待用户决定是否做**。**另一处易被误判为"result 没更新"**：`result.json` 是 `<CWD>` 下的**固定名**，连跑多轮互相覆盖（契约已明载），故 C++ 那一轮 84 笔的结果会被其后任一 Python 轮次的 0 覆盖——想留档须先把它挪走。
  - **产物清理提示（同日）**：上一轮引用的 `Dump/20260918_180845_955` 已被清理，本轮证据统一改用仍在的 `Dump/20260918_194031_140`（同一场景、同样的 420.0 / 4.297395 / 1.341961）。
  - **审查与遗留（2026-09-18，`code-reviewer`）**：`on_bar` 与 C++ `OnBar` 逐条对齐、属性名 `bar_data.close` 正确、插入位置合规，**0 严重 0 高危**；已按建议补一行 docstring 说明"Bar 模式无 tick 回调"。① **DRY（已定案：不抽）**——`on_tick`/`on_bar` 有 5 行完全相同的锚定体，按 Harness §5 本应抽 `anchor_ladder(anchor_price)`，**用户 2026-09-18 裁定不抽**：C++ `GridStrategy` 同样是两路重复，只抽 Python 会破坏本任务刻意要求的"与 C++ 1:1 对应"，且抽了要连带动 C++，收益不抵。**缺陷留在此处仅作记录，不再作为待办。**② **无效价哨兵**、③ **绑定层静默** 两项已移入 ❓ 段。另注 `on_bar` 未加类型注解、沿用全文件的 `%` 格式化，均属文件既有风格，**刻意不为本次改动单点破例**（避免同文件两种风格并存）。
  - **风险（§7）**：无多线程/锁/内存管理改动。`AccumulateAccountFee` 遍历该账户全部资金行（62 行量级）、只在收尾调用一次，非热路径。**属破坏性契约变更**：三个键改名 + 新增一个键、`SchemaVersion` 1 → 2；**当前无任何消费者读这些键**（两个 C++ 宿主与 Python 宿主只读 `Success`），故零迁移成本。
  - **注释披露（§4）**：新增 3 处一句话注释，均为"命名无法表达的口径"，全在 `RunResult.h`（`MarketDataType` 取值决定哪个计数有意义、期末值、整轮累计）。初稿在 `SimExchange.h` 另有一处解释 `AccumulateAccountFee` 为何要跨行相加，经 `code-reviewer` 审查判定与该处注释是同一事实的重复复述（且函数名已表达"跨行累加"），**已删除**。**剩余 3 处如需删除请指出。**
  - **未验证**：**AI 未编译**（按约定，构建在 VS 侧）。已用 Python 直接核对产物 CSV 的三种求和（`t_Trade`/`t_PositionDetail`/`t_Capital` 均为 420.000000），但 C++ 侧改动尚待用户重跑验证。补记的 `on_bar` **已 `py_compile` 通过**，并用桩对象实跑验证了原逻辑：`close=100.0`、`GridStep=10`、`GridCount=5` → 挂 10 笔（买 90/80/70/60/50、卖 110/120/130/140/150），再喂一根 bar 不重复挂——与 C++ `PlaceLadder` 同构；**尚未经引擎端到端跑**（需用户重跑 Python 宿主确认 `OrderCount > 0`）。改动已同步到 `bin/Release/grid_strategy.py`（构建产物，CMake 下次构建会再拷一次）。
  - **提交状态**：**已提交** `fcb80d7`（2026-09-18，7 个文件、+122/−50；含 `PROGRESS.md`/`PROGRESS-archive.md` 的本次记账）。`Model/TableNames/SimExchangeTableNames.xml` 依旧未改。**AI 未推送**（按约定）。

## 🔄 进行中

- **回测平台化（2026-09-17 起；引擎侧 A3/A4/A5 已交付，见 ✅ 第十五批）**：形态、RunId 归属、产物布局三条已定案（见归档 `D.48`），平台化的三块硬骨头已于 2026-09-18 一次性落地，**剩余全在调度侧**：
  1. **catalog 由调度侧写**（未开工）：参数（是它自己写的配置）+ RunId + 产物 URI + 退出码 + 耗时 + 引擎回报的指标——指标全部读 `<job 工作目录>/result.json`（键表见 `docs/backtest-run-contract.md` §2）。**引擎不碰 catalog**，回测侧零改动。
  2. **调度层本体**（未开工）：收请求 → 落 run 记录 → 队列 → worker 拉起宿主进程 → 收集退出码与 `result.json`。宿主先只收 Python（见下"平台形态补充"）。**必须为每个 job 分配独立工作目录**——引擎的全部输入与产物路径都相对 CWD，这是隔离的全部依据（契约 §1）。
  3. **跨运行对比**：**暂不做**。多数对比只需 catalog 的指标行；真要做时再决定导 Parquet（`read_parquet('runs/*/order.parquet', filename = true)`，RunId 由**读侧**从路径派生，写入侧不加字段）还是 SQLite `ATTACH`（`SQLITE_MAX_ATTACHED` **默认上限 10**，比 20 次运行就得改编译期上限或应用层循环）。
  4. **形态定案（2026-09-18 用户拍板）**：**"调度层"就是 web 后端**，不是另一个程序——收请求、建 job 工作目录、拉起回测进程、收退出码与 `result.json`、写 catalog 都由它做。由此**web 后端必须与回测引擎同机**（要读行情磁盘、要用 cp311 的 `.pyd` 拉起宿主）。产物**不上对象存储，直接落本机磁盘用 SQLite**（catalog 与产物同机，跨运行对比=查同一个库；`ATTACH` 上限 10 只影响「一次并把很多轮」的写法）；**并发上限由 web 侧控制**，调度层与引擎都不决策。行情路径将来由 **QuantHub 组件**更新（走相对路径）。
  5. **上平台剩余四件（按依赖顺序）**：① **result 口径**——已由 ✅ 第十六批修掉；② **job 工作目录的构造与 `.pyd` 查找**（未开工，**硬钉子**）——`test/PythonStrategyGrid/grid_strategy.py:13-14` 靠 `__file__` 反推 `REPO_ROOT/bin/Release` 来找 `QuantTrading.cp311-win_amd64.pyd`，**脚本一进独立工作目录就 import 失败**，须先定「策略脚本与 `.pyd` 放哪、怎么被找到」；③ **runner 本体**（未开工）——拉进程、限时、收 stdout/stderr、读 `result.json`、写 catalog；坑位是 web 重启会留孤儿任务，启动时须扫「状态=运行中但进程已不在」标为失败；④ **catalog 表结构**（未开工）——平台侧字段 + 照抄 `result.json` 的键，等 ②③ 跑通再定。
  6. **形态二选一（未定）**：web 后端**自己 `subprocess` 拉起**回测（少一个进程，代价是 web 重启留孤儿任务，需补一段启动清理）vs **拆一个本机 worker 服务**从 SQLite 任务表取任务（web 可随便重启，代价是多一个常驻自启进程）。**推荐前者**，等 web 需要频繁重启或独立部署时再拆。
  - **本批留下的尾巴（① 已了结，②③ 未决；细节见 ✅ 第十五批）**：① **模板漂移已了结（2026-09-18）**——命名漂移（`requestID`）由用户在源头修复（`D:\Gitee\Templates` 的 7 个 Cpp 模板改回 `requestId`，本仓已核对零残留）；BOM 经查为**既定约定**而非漂移（`pump.py` 生成产物即用 `UTF-8-SIG` 写、仓根另有专职的 `ConvertToUtf8Bom.py`），用户裁定 18 个纯 BOM 文件**原样提交**，详见归档 `Q.21`；② **实盘不计费边界**——`SimExchangeTableNames.xml` 刻意未改，实盘侧 `BaseCommission` 恒 `nullptr`、三列费用恒 0，**需用户确认接受**（见 ❓）；③ **`CommissionRate` 字段 id 序列前移**（`0x5006`–`0x500D` → `0x5002`–`0x5009`，因仓外字典里 `0x500A`–`0x500D` 被删、id 集整体前移）：本仓两端同批重生成、自洽，**仓外若有老对端进程会按 id 解错槽位含义**；本批不做协议版本号。
  - **基本数据 / InitDb（2026-09-17 定案走甲，2026-09-18 落地）**：通路已存在（生产者 `src/SimExchangeInit/`，消费者 `InitMdbFromDb::LoadTables`，配置项 `DbInitHost`），回测侧本批已接上（见 ✅ 第十五批）。**仍开放**：行情**盘后修正**（同一 `TradingDay` 多版本）性质与合约元数据无关，以后再说。**乙（否决留档，切换条件保留）**：InitDb 供 `Instrument` + 把 `InitMdInstrument` 改成"Product 命中才派生、未命中且已有行则跳过不覆盖"——收益是合约粒度更准，代价是引入来源判定，且行情侧将来真补上那 9 列时要再打一次架；**parquet 侧带上合约元数据列时再评估**。
  - **平台形态补充：策略宿主与"先只收 Python"（2026-09-17 定案）**：平台拉起的是**用户策略程序**而非"BackTest.exe"——引擎是库（`BackTest.dll`），`test/TestBackTest`、`test/TestStrategyGrid` 是两个宿主实例，Python 侧宿主是解释器（`test/PythonStrategyGrid/` 只有 `grid_strategy.py`，`QuantTrading.cp311-win_amd64.pyd` 被 CPython import）。宿主读两个 **cwd 相对** JSON（自己的 `TestBackTest.json` + 引擎硬编码的 `BackTest.json`，`BackTestApiImpl.cpp:10`），且 `argv` 未使用——对"每 job 独立工作目录"是好事，但调度侧须知道宿主配置的文件名。**定案：先只收 Python**——Runner 退化为"固定 Python 启动器 + 固定契约"（配置名、退出码、`result.json`），不需 DLL 加载、不发 SDK、无 ABI 问题。**"用户编成库 + Runner 加载"的 C++ 方案暂缓**（用户曾提议），理由：编译负担只是转给用户；新引入跨 DLL 边界脆弱性（须钉死同 CRT、同侧分配释放、不跨边界抛异常、单例唯一——`Config`/`Logger` 均为 `GetInstance()` 进程级单例，策略 DLL 静态链会拿到副本）；版本错配表现为**踩内存而非加载失败**（`BackTestApi.h` 是生成文件）；in-process 任意代码执行不比独立 exe 安全；且 Python 路径本就不需要它。**再评估 C++ 的三个触发条件**：策略性能卡住 / 已有 C++ 策略要迁 / 用户不肯交源码。

- **（保留的已关闭条目，非待办）实时行情周期**：2026-09-13 已定案：**实时链路恒出 1m、不聚合是有意设计**，剩余的前置约束记入下方备注。⚠️ 曾提议的"`FieldsCompare` 比较器补齐 `BarPreces`/`BarPeriod`"**已撤回**——该比较器是"合约身份键"，补齐周期会让 `PushToAllSubscribed` 的 0 周期探针匹配不到带周期入集的会话，**直接中断行情推送**；撤回证据见归档 `R.01` 与归档 `D.36`。

## ❓ 待讨论 / 待决策

> 已关闭/已了结的条目（`Q.00`–`Q.15`、`Q.16`、`Q.19`、`Q.20`、`Q.21` 等）已归档；下面只留未决项。

- **策略锚价的无效价哨兵可能在 `+inf` 上失效（2026-09-18 第十六批审查中发现，两侧同源，待决策）**：`GridStrategy`/`grid_strategy.py` 的锚价判据是——tick 路 `price <= 0 || price == std::numeric_limits<PriceType>::max()`（Python 侧 `== sys.float_info.max`），bar 路只判 `<= 0`。而 `MdReader` 的向量化读对 NULL 映射为 **`Double = +inf`**（`src/BackTest/MdReader.cpp:185-190` 注释明载），**`+inf` 与 `DBL_MAX` 不是同一个值**，故 tick 路那条 `== max` 判据对 NULL 来源的价格**无效**；bar 路的 `<= 0` 同样拦不住 `+inf`。后果：若某根 bar/tick 的价格真为 NULL，锚价被锚成 `+inf`，`place_ladder` 算出的挂单价全是 `inf ∓ step`，整日锁定错误中枢。**本轮数据未触发**（84 笔那轮锚定正常，`Close` 在 Bar SQL 里是 parquet 真实列而非占位 NULL）。补判据要**同步改 C++ 与 Python 两侧**，且该判据的归属（留在各调用点 vs 收进公共锚定函数）与已定案的"不抽函数"决定相扣，故留待决策。
- **Python 绑定层对"未覆写钩子"静默降级（2026-09-18 第十六批中发现，待决策）**：`src/PythonBindings/QuantTradingBindings.cpp:143` 的 `DispatchHookWithGil` 在 `py::get_overload` 返回空时**直接走基类空实现、不打任何日志**。这正是"Python 策略漏写 `on_bar`、在 `MatchMode: Bar` 下静默 0 成交"能潜伏数轮的原因（见 ✅ 第十六批「补记」）。建议按**钩子名只告警一次**（Bar 模式下 `on_bar` 会被调 2928 次，逐次告警会刷屏），或仅对"引擎确实投递了该钩子"的路径告警。属共享绑定层行为变更，需先定告警的触发条件与节流方式。
- **实盘侧不计费（2026-09-18 第十五批划的边界，需用户确认接受）**：`Model/TableNames/SimExchangeTableNames.xml` 本批**刻意未改**——它被实盘 `src/SimExchange/` 与 `src/SimExchangeInit/` 共用（`pumplist.xml` 里一个 model 两个 dest），改它会动实盘安装载路径与产物。代价是实盘侧 `mdb->BaseCommission == nullptr`、`CommissionCalculator::Apply` 空守卫直接返回、三列费用恒 0，**实盘行为逐字节不变但也不计费**。要实盘计费需给实盘单独注册这两张表，并确认实盘库中确有 `BaseCommission` 行。

- **`FieldType` 扩展留下的 ❓（第十二批，2026-09-14；批次原文见归档 `D.47`，以下均属"刻意不纳入本次"）**：
  - **① 模板侧编译期宽度断言（建议单独一个提交做）**：模板已能拿到成员的 C++ 类型（`MdbStructs.h.tpl:29` 的 `!!@type!!Type`），可为每个字段生成 `static_assert(sizeof(S::F) == N, ...)`，把"描述符宽度 vs 成员宽度"一整类潜伏缺陷挪到编译期（含"将来有人改 enum basetype"的隐患）。代价是三个仓生成物各多 N 行，会掩盖第十二批"diff 只含改名"的验收，故本次不做。
  - **② `SqliteWrapper.cpp` 的 `sqlite3_bind_text(..., -1, ...)`**：`-1` 依赖 NUL 终止，而读路径在列值满宽时不写终止符，于是"查出来再写回去"会越界读。修法 `strnlen(data, field.arraySize)` + `SQLITE_TRANSIENT`，属存量语义变更。**这条已实测咬到第十二批的夹具设计**——故该批明确要求"`Char` 字段不可与窄整数相邻"。
  - **③ Char 满宽不终止的跨后端不一致**：SQLite/MySQL/MariaDB 三支在列值满宽时不补 `\0`，只有 `DuckdbWrapper::ReadCellAsChar` 做了截断 + 补零。
  - **④ `Templates/SQL/Duckdb/CreateTables.sql.tpl` 的 `uint16s → 'int'`**（应为 `usmallint`）：只影响"表由 SQL 脚本建、由 Wrapper 读"时的源类型判断，写入无损。
  - **⑤ 存量库表的迁移策略**（`ALTER` 脚本 vs 直接重建 vs 接受漂移）未定；与第十二批风险 ⑤ 同源。
  - **⑥ Duckdb 结果级读取走的是已废弃的 "Safe Fetch Functions" 组**（`duckdb_value_int64`/`uint64` 等）：`duckdb.h` 推荐改用 `duckdb_fetch_chunk`。第十二批为了让两条读路径都正确，在结果级路径上沿用了该组，属"跟随现状"，未迁移。
  - **⑦ `ReadCellAsInt64` 对 `UBIGINT` ≥2⁶³ 用 `static_cast<long long>` 转换**：C++20 下是模运算（实现定义 → 已定型为取模），值会变成负数。第十二批已让 `UInt64` 目标走独立的 `ReadCellAsUInt64` 绕开它，但**其他以 `Int64` 为目标的窄位宽读路径仍可能命中**，待核查。
  - **⑧ `DuckdbWrapper.cpp` 的 `ReadCellAsInt32` 已成死代码**（第十二批把结果级读合并为一条整数臂后无调用者）：**刻意未删**，按 Harness §3.1 删除导出符号须先经用户确认，待用户裁决。
  - **⑨ Mysql / Mariadb 两支无往返验证**：本地无服务器，第十二批只做了编译覆盖。

- **入站包方向与鉴权校验缺失（半关闭；2026-09-13 第九批落地网络侧"不收"）**：`PackageFactoryBase::IsInboundPackageAccepted` + `PackageReader` 两条解析路径过滤，反向包在 `CreatePackage` **之前**即被拒（生成代码按类名前缀推导方向，52 包全分类、无 fail-open）；内核侧"少认"见归档 `D.40`（第六批分发生成）。**仍未解决**：① 会话合法性——`NotifyDisConnect` 取的是**报文体**里的 `SessionId`、可指定他人会话（与方向正交），等用户定的会话登录检查；② 候选方案 (a)（由 `PackageNames/*.xml` 生成运行期查表）与 (b)（`Packages.xml` 加 `direction` 列）未采纳但仍在。完整侦察记录（网络层全貌核对、执法点论证、第二层过滤治不了的部分、三件待拍板）见归档 `Q.16`。**2026-09-13（第十批）复核：① 的两条线均已关闭**——伪造方向这条，`Notify` 前缀在生成的方向表里一律 `acceptExpr = "false"`（网线上永不合法，包对象创建前即被拒），且内核侧唯一的 `NotifyDisConnectPackage` 生产者是 `SimExchange::OnProtocolDisConnect` 与 `MdKernel::OnProtocolDisConnect`，二者都自造且 `SessionId` 取传输层真值，故"报文体 SessionId 可指定他人会话"不再可达；会话登录检查这条，本批已落地（交易所查 `t_PrimaryAccountLoginSession`、行情查 `t_MdUserLoginSession`，见归档 `D.45`）。② 的 (a)/(b) 是当时未采纳的备选，非待办。**本条已无未决内容，待用户确认后入归档**（原侦察记录在归档 `Q.16`）。
- **`PrimaryAccount.IsAllowLogin` 只被写入、从未被读（2026-09-13 记录；原"登录路径两个小缺口"的 ① 已修，见归档 `D.46`）**：`Model/Tables/Tables.xml:270` 的该字段全仓**只被写入、从未被读**——播种器 `src/SimExchangeInit/Init.cpp:69` 无条件置 `true`，生成代码只在字段注册与 `Dump` 里出现（`src/Mdb/MdbStructs.cpp:510/531/537/541`），没有任何一处参与登录判定，故 `IsAllowLogin = false` 的主账号**仍能登录成功**。**候选修法**：在密码校验后补一行 `if (!primaryAccount->IsAllowLogin) { errorId = ErrorAccountForbidden; }`（`ErrorAccountForbidden` = 0xA001，已存在），属登录路径上的一行守卫。用户 2026-09-13 指示暂缓。
- **数据源整理对齐 mdb**（用户负责）：TestBackTest 已能在旧格式 parquet（`LastTraded`/`LastTurnover`/数组盘口，缺 OpenPrice/ClosePrice/Upper/LowerLimitPrice/AveragePrice 5 列）上端到端跑通，**靠 MdReader SQL 的 NULL 占位 + 旧列名兜底**；数据侧未真正对齐 mdb schema。真正对齐后 SQL 可删掉占位符，且 tick 的涨跌停价列才真实可用（当前 OrderMatch 的涨跌停校验处于注释状态，`GetSettlementPrice` 对 +inf 有回退，故暂不影响撮合/结算正确性）。
- **P2-1 MdKernel 职责拆分**：`HandleRtnDepthMarketData` 同时做 bar 聚合/Mdb 更新/快照/广播，建议预留 tick 处理管线。
- **P2-2 Bar 内存策略**：`MinuteBar` 用裸 `new` 且 `todayBars_` 无日界清理，考虑改对象池 + 日界重置。
- **`SimExchange::HandleReqAccountLogin` 未判空即解引用（2026-09-12 网络层核对时发现，与 C3 同级，未修）**：`src/SimExchange/SimExchange.cpp:197` 直接取 `reqPackage->ReqAccountLogin->AccountId` 作为 `t_PrimaryAccount` 的查询键、`:202` 再取 `->Password` 做 `strcmp`（函数名与行号 2026-09-13 复核校正：第六批已改名 `HandleReqAccountLogin`，原记 `:232`/`:240`），**全程无判空**。而 `ReqAccountLoginPackage`（`0x2003`）与 MdKernel 的登录包同属"只挡多余字段、不校验缺失字段"的生成代码（见下一条模板条目），故只有包头（`BodyLen=0`）的登录报文会让字段指针保持 `nullptr`：`MdKernel::HandleReqMdUserLogin` 在第五批已补判空（`MdKernel.cpp:223`），**SimExchange 这半边漏了**。可达性：`0x2003` 是登录请求本身，无需任何前置状态即可到达。后果：未鉴权可达的空指针解引用（`Select` 与 `strcmp` 各一次）。修法照抄 MdKernel 的判空分支即可（`errorId` 保持初始值 + 记 Warning）。**同类未修**：`SimExchange::HandleReqAccountLogout`（`:225` 起，唯一字段区解引用在 `:230` 的 `reqPackage->ReqAccountLogout->AccountId`；原记 `:270` 有误，2026-09-13 复核校正）、`HandleInsertOrder`/`HandleCancelOrder`/`HandleQry*` 的字段区解引用尚未逐一核对（本次只核到登录处）。另记：`src/SimExchange/SimExchange.cpp` 共 12 处裸 `strcpy`（`:27/:86/:256/:258/:270/:277/:282/:452/:454/:464` 等），但核对后**两侧同宽**（`AccountIdType ← AccountIdType`、`ErrorMsg ← 字面量`），与 C4 的"项目类型 → 更窄的 CTP 类型"不同级，属 `rules/cpp-style.md` §6 风格问题，非内存安全缺陷。**2026-09-12（第六批）可达路径已封**：上述 7 个包全部登记进 `SimExchangeKernelPackageNames.xml` 且以 `Req` 开头 → 生成的分发守卫在进入 Handler 前即对空字段区回绝（`ErrorReqPackageParseFailed`），**网线来的空指针不再可达**（这 7 个包在 SimExchange 侧只从网线来，无内部产生者）。Handler 内部仍无判空，作为纵深防御缺口保留记录；建议后续按同一清单逐条补判空（或由"生成期必填字段区"的模板级修法一次解决，见下一条）。
- **生成模板级"缺字段区不校验"（2026-09-12 记录，待决策）**：`src/Packages/Packages.cpp` 由 `D:\Gitee\Templates` 的 `Cpp/Protocol/Packages/Packages.cpp.tpl` 生成，48 个字段区一律"只在遇到 FieldID 时分配、函数尾无条件 `return true`"，故**每个包的必填字段区缺失都靠消费方判空兜底**（本批已在 MdKernel 补 5 处）。模板级修法是让"唯一字段区"的包在末尾 `return <字段指针> != nullptr;`——失败即表现为解析失败，由 Spark `Protocol.cpp` 走既有 `DisConnect(sessionId)` 路径断 TCP，消费方无需判空。**未做**：改模板需重 pump 全部生成文件并核对，且会影响所有消费方（含测试客户端），属跨仓库改动，待用户决策走模板还是维持"消费方判空"的现状。
- **解析失败路径上已分配的 package 未回收（待决策，属生成层 + Spark 侧）**：`Protocol::OnRecv` 解析失败即断 TCP，但失败前已 `CreatePackage` 出来的对象无人释放，可耗对象池——2026-09-13 复核：`src/Network/Protocol/{PackageReader,Protocol}.cpp` 里 `Deallocate` 只用于 `PackageReader` 自身（`:35`、`Protocol.cpp:131`），**没有一处回收 package**，原记录成立。第九批的网络侧过滤只挡在 `CreatePackage` **之前**（被拒 ID 不分配），未触及这条失败路径。同类但不同源：`src/BackTest/SimExchange.cpp:500` 的 `continue` 跳过 `:535` 的 `::Deallocate(reqSubMd)`（归档 `R.00` ③，属 `fd7afef init` 既有）。
- **`ReqSubMarketDataField` 未绑定到 Python（待决策，2026-09-12 记录）**：补 `BarMarketDataField` 的 `BarPreces`/`BarPeriod` 时留下的尾巴——它不属策略面向 API（Python 侧声明周期走 `declare_bar_period("5m")` 字符串，与 C++ 签名一致），故当时未绑。完整记录见归档 `Q.19`。
- **Python 绑定构建前置未就绪（2026-09-12 记录）**：`CMakeLists.txt:269` 走 `python -m pybind11 --cmakedir`，要求目标解释器已 `pip install pybind11`；本机 Python 3.14.5 无该模块，而 `Libs/pybind11/2.13.4` 只有 `include/` + `pybind11/` python 包（无 `share/cmake`）、`Libs/python/3.12` 只有 `include/`（无 libs/exe），都无法替代。**发布相关**：`.pyd` 受解释器 ABI 版本锁定（本机编出的只能被对应次版本导入），若 Release 包内含 `QuantTrading.pyd`，需在 README 写明目标 Python 次版本，或作为可选组件单独提供。

## 备注

- **股票回测 0 委托 0 成交（2026-09-18 观察，当日已定案，非引擎缺陷）**：曾表现为"同一场景有时 84 成交、有时 0 成交"（`Dump/20260918_194031_140` 是 654 委托 / 84 成交，`Dump/20260918_180912_511` / `:194058_440` / `:194155_711` 都是 0）。**定案**：不是随机、是**按宿主分**——C++ 宿主 `TestStrategyGrid` 每轮都有成交，Python 宿主 `grid_strategy.py` 每轮都没有，根因是该脚本**只定义了 `on_tick`、缺 `on_bar`**，而 `MatchMode: Bar` 下引擎从不回调 `OnTick`。**已修**（补 `on_bar`，与 C++ `OnBar` 同构），详见 ✅ 第十六批「补记」。**引擎报的 0 是忠实的**；日志侧 `Anchor price` 出现 0 次即其表征。
- **种子库行数与计划不符（2026-09-18 第十五批核对说明，非缺陷）**：计划 §验证 写「新增 `dumpPath_/t_BaseCommission.csv`（4 行）」，实际内置种子是 **6 行**（3 只标的 × 买卖两向），`t_CommissionGroup.csv` 1 行、`Product` 2 行。差异源于计划拟定时的口径（按 2 只标的估），非实现偏差。
- **实时链路恒出 1m、不消费订阅声明的周期（2026-09-13 定案：有意设计，非缺陷；剩余项为将来实盘宿主的前置约束）**：`MinuteBar` 恒定产出 `Minute/1`（`src/Bar/MinuteBar.cpp:116-117` 与 `:151-152` 硬编码；`test/UnitTests/MinuteBarTests.cpp:103-104` 已把 1m 断言成契约），`MdKernel::HandleReqSubMarketData` 不读包里的周期（`src/MdOffer/MdKernel.cpp:302` 只取 `ExchangeId`/`InstrumentId`，`MinuteBar::ReqSubMarketData` 签名也不含周期，`src/Bar/MinuteBar.h:18`），而 `ReqSubMarketDataField` 本身是带 `BarPreces`/`BarPeriod` 的（`include/QuantTrading/Fields.h:73-74`）——故这两个字段在**实时侧无效**。**今天不可达**：全仓唯一给它们赋非零值的是 `StrategyBase::SubscribeMarketData`（`src/Strategy/StrategyBase.cpp:46-47`），而 `StrategyBase` 硬绑 `BackTestApi`（`src/Strategy/StrategyBase.h:19/98`，:10 注释写明"实盘路径（MdApi+TraderApi）"尚未落地），走的是回测侧——那侧才读周期并绑 `BarAggregator`；实时侧两个调用者都发 0（`test/TestMdApi/MdSpiImpl.cpp:11-12` 的 `memset`、`src/SimExchange/SimExchange.cpp:522` 的 `reqSubMd{0}`）。**前置约束**：将来给 `StrategyBase` 接 MdApi 宿主时须先定——要么约定实时只支持 1m 并在 `SubscribeMarketData` 处拒绝/告警非零周期（否则声明 5m 会被静默丢弃、仍回 `ErrorNone`），要么把聚合放到宿主侧。**已否决**：让 MdOffer 自己做多周期聚合（须重做 `PushToAllSubscribed` 的 0 周期探针匹配，风险见本文件 🔄 段的撤回记录）。
- **待推送（按约定 AI 不推送，由用户执行）**：**2026-09-18 晚复核，`QuantTrading` master 领先 `origin/master` 1 笔**（`fcb80d7` result.json 口径修正 + Python 策略补 `on_bar`）。此前 `a25d4c1`（回测 RunId 改为配置注入）、`4fab69c`（回测平台化 A3/A4/A5）与一笔 PROGRESS.md 补记**已由用户推送**。2026-09-14 复核时，`QuantTrading` 领先 `origin/master` 2 笔、`D:\Gitee\Templates` master 领先 4 笔（含方向过滤、会话表过滤、CSV 会话表过滤三份模板，现又加归档 `D.47`（第十二批）的 `MdbStructs.cpp.tpl` 改动，**该仓仍按用户指示不提交**）。
- 提交信息历史多为 `1`，建议后续写描述性提交信息。
- `D:\Gitee\Templates` 仓库的模板改动（S1/S3/H10/H12 对应 `.tpl`）已提交（`6d12e3d`）。
- `rules/cpp-style.md` 成员命名要求 snake_case，现有代码为 `m_` + PascalCase，项目自洽但与规范不一致（待统一）。
